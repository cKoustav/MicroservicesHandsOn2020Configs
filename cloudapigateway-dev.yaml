spring:
  cloud:
    gateway:
      routes:
      - id: managecurrencyconversionfactor
        uri: lb://managecurrencyconversionfactor
        predicates:
        - Path=/**
        
      - id: convertcurrency
        uri: lb://convertcurrency
        predicates:
        - Path=/currency/**